//
//  MultipleSelectViewController.swift
//  JS
//
//  Created by 郭 芷瑄 on 2018/2/3.
//  Copyright © 2018年 Json.test1. All rights reserved.
//

import UIKit

class MultipleSelectViewController: UIViewController {

    
    @IBAction func basketball(_ sender: Any) {
//        if let button = sender as! UIbutton {
//            if let image = UIImage(named:"Unchecked") {
//                button.setImage(UIImage(named:"testImage.png"), forControlState: .Normal)
//            }
//            if let image = UIImage(named:"Checked") {
//                button.setImage( UIImage(named:"testImage1.png"), forControlState: .Normal)
//            }
//
//        }
    }
    
    
    @IBAction func volleyball(_ sender: Any) {
    }
    @IBAction func baseball(_ sender: Any) {
    }
    @IBAction func softball(_ sender: Any) {
    }
    @IBAction func kickball(_ sender: Any) {
    }
    @IBAction func jogging(_ sender: Any) {
    }
    @IBAction func badminton(_ sender: Any) {
    }
    @IBAction func workout(_ sender: Any) {
    }
    @IBAction func golf(_ sender: Any) {
    }
    
    
    
    
    override func viewDidLoad() {
        super.viewDidLoad()

        // Do any additional setup after loading the view.
    }

    override func didReceiveMemoryWarning() {
        super.didReceiveMemoryWarning()
        // Dispose of any resources that can be recreated.
    }
    

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destinationViewController.
        // Pass the selected object to the new view controller.
    }
    */

}
